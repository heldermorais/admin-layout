<button class="btn btn-${attrs.size} ${ attrs.color ? 'btn-'+attrs.color : 'btn-default' }" type="${ attrs?.type ? attrs?.type : 'button'}" <g:if test="${attrs.tooltip}"> data-toggle='tooltip' title="${attrs.tooltip}" data-placement="${attrs?.tooltippos ? attrs?.tooltippos : 'top' }"</g:if> <g:each in="${attrs?.ngOptions}"> ${it.key}="${it.value}" </g:each><g:each in="${attrs?.htmlOptions}"> ${it.key}="${it.value}" </g:each>> <i class="icon-${attrs.icon}"></i> ${attrs.label}</button>